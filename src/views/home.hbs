<section id="catalog">
    <h1>All Excursion</h1>

    <div class="band" style="background-color: rgb(0, 0, 0);">   

        {{#each movies}}
            {{>movie}}
        {{else}}
            <div class="no-posts">
                <p class="no-offer">There are no excursions matched...</p>
            </div>
        {{/each}}     

    </div>
</section>